# from app.schemas.emission import EmissionRequest
# from app.models.emission_model import model
# from app.utils.preprocessing import preprocess




# def predict_emission(data: EmissionRequest):
#     features = preprocess(data)
#     prediction = model.predict(features)
#     return {"predicted_emission": float(prediction)}